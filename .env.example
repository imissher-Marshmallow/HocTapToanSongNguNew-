# Example environment variables (do NOT commit real secrets)
# IMPORTANT: replace with your own key locally in a `.env` file. This example must not contain real keys.
OPENAI_API_KEY=your_openai_api_key_here
PORT=5000
